@using GroundHouse.ViewModels;
@using GroundHouse.Models;
@using Microsoft.AspNetCore.Identity;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@*it is a directive to include all tagHelpers(* stays for all)*@

@*We actually could put these all in _ViewStart.cshtml but it is a good practice to put them in specific file*@